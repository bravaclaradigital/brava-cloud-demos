name: Deploy Azure Landing Zone

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        default: 'apply'
        type: choice
        options:
          - apply
          - destroy

env:
  TERRAFORM_VERSION: 1.5.0
  AZURE_REGION: eastus

jobs:
  terraform:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./azure-landing-zone-tf
    
    permissions:
      contents: read
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}
      
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      
      - name: Extract Azure Credentials
        id: azure-creds
        run: |
          echo "subscription_id=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.subscriptionId')" >> $GITHUB_OUTPUT
          echo "client_id=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.clientId')" >> $GITHUB_OUTPUT
          echo "client_secret=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.clientSecret')" >> $GITHUB_OUTPUT
          echo "tenant_id=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.tenantId')" >> $GITHUB_OUTPUT
      
      - name: Terraform Init
        env:
          ARM_CLIENT_ID: ${{ steps.azure-creds.outputs.client_id }}
          ARM_CLIENT_SECRET: ${{ steps.azure-creds.outputs.client_secret }}
          ARM_SUBSCRIPTION_ID: ${{ steps.azure-creds.outputs.subscription_id }}
          ARM_TENANT_ID: ${{ steps.azure-creds.outputs.tenant_id }}
        run: terraform init
      
      - name: Terraform Format Check
        run: terraform fmt -check -recursive
        continue-on-error: true
      
      - name: Terraform Validate
        run: terraform validate
      
      - name: Terraform Plan
        env:
          ARM_CLIENT_ID: ${{ steps.azure-creds.outputs.client_id }}
          ARM_CLIENT_SECRET: ${{ steps.azure-creds.outputs.client_secret }}
          ARM_SUBSCRIPTION_ID: ${{ steps.azure-creds.outputs.subscription_id }}
          ARM_TENANT_ID: ${{ steps.azure-creds.outputs.tenant_id }}
        run: terraform plan -out=tfplan -lock=false -var="azure_subscription_id=${{ steps.azure-creds.outputs.subscription_id }}"
      
      - name: Comment PR with Plan
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync('./azure-landing-zone-tf/tfplan', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: ## Terraform Plan - Azure Landing Zone\n\\\\n${plan}\n\\\`
            });
  
  deploy:
    runs-on: ubuntu-latest
    needs: terraform
    if: github.ref == 'refs/heads/main' && github.event.inputs.action == 'apply'
    defaults:
      run:
        working-directory: ./azure-landing-zone-tf
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}
      
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      
      - name: Extract Azure Credentials
        id: azure-creds
        run: |
          echo "subscription_id=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.subscriptionId')" >> $GITHUB_OUTPUT
          echo "client_id=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.clientId')" >> $GITHUB_OUTPUT
          echo "client_secret=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.clientSecret')" >> $GITHUB_OUTPUT
          echo "tenant_id=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.tenantId')" >> $GITHUB_OUTPUT
      
      - name: Terraform Init
        env:
          ARM_CLIENT_ID: ${{ steps.azure-creds.outputs.client_id }}
          ARM_CLIENT_SECRET: ${{ steps.azure-creds.outputs.client_secret }}
          ARM_SUBSCRIPTION_ID: ${{ steps.azure-creds.outputs.subscription_id }}
          ARM_TENANT_ID: ${{ steps.azure-creds.outputs.tenant_id }}
        run: terraform init
      
      - name: Terraform Apply
        env:
          ARM_CLIENT_ID: ${{ steps.azure-creds.outputs.client_id }}
          ARM_CLIENT_SECRET: ${{ steps.azure-creds.outputs.client_secret }}
          ARM_SUBSCRIPTION_ID: ${{ steps.azure-creds.outputs.subscription_id }}
          ARM_TENANT_ID: ${{ steps.azure-creds.outputs.tenant_id }}
        run: terraform apply -auto-approve -lock=false -var="azure_subscription_id=${{ steps.azure-creds.outputs.subscription_id }}"
  
  destroy:
    runs-on: ubuntu-latest
    needs: terraform
    if: github.ref == 'refs/heads/main' && github.event.inputs.action == 'destroy'
    defaults:
      run:
        working-directory: ./azure-landing-zone-tf
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}
      
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      
      - name: Extract Azure Credentials
        id: azure-creds
        run: |
          echo "subscription_id=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.subscriptionId')" >> $GITHUB_OUTPUT
          echo "client_id=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.clientId')" >> $GITHUB_OUTPUT
          echo "client_secret=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.clientSecret')" >> $GITHUB_OUTPUT
          echo "tenant_id=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.tenantId')" >> $GITHUB_OUTPUT
      
      - name: Terraform Init
        env:
          ARM_CLIENT_ID: ${{ steps.azure-creds.outputs.client_id }}
          ARM_CLIENT_SECRET: ${{ steps.azure-creds.outputs.client_secret }}
          ARM_SUBSCRIPTION_ID: ${{ steps.azure-creds.outputs.subscription_id }}
          ARM_TENANT_ID: ${{ steps.azure-creds.outputs.tenant_id }}
        run: terraform init
      
      - name: Terraform Destroy
        env:
          ARM_CLIENT_ID: ${{ steps.azure-creds.outputs.client_id }}
          ARM_CLIENT_SECRET: ${{ steps.azure-creds.outputs.client_secret }}
          ARM_SUBSCRIPTION_ID: ${{ steps.azure-creds.outputs.subscription_id }}
          ARM_TENANT_ID: ${{ steps.azure-creds.outputs.tenant_id }}
        run: terraform destroy -auto-approve -lock=false -var="azure_subscription_id=${{ steps.azure-creds.outputs.subscription_id }}"
